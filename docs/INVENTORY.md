# Ignacio Product CV inventory

## Known contents
- Vite + React application source in `src/`
- Build configuration in `vite.config.js` and `tailwind.config.js`

## Structural note
- This repo is an active source app, not an archive shell.
